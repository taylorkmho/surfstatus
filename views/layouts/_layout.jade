include ../_modules/_Modules

block vars
  - var bodyTemplate = "default"

doctype html
html
  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title #{title}
    link(rel='stylesheet' href='../css/app.css')
    block addToHead
  body(data-template="#{bodyTemplate}")

    block content

    footer.suggestion
      h1.suggestion__message It's Monday, you should <span>call in sick</span>.

    block footerScripts
    script(src="../js/vendor/jaaulde-cookies.js")
    script(src="../js/app.js")